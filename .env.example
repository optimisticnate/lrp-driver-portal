###########################################
# Environment variables (example values)    #
# Rename this file to `.env` and replace    #
# placeholders with your real credentials. #
############################################

# TODO: Move client secrets to server-side environment variables.

# Google Calendar API configuration (client-side)
VITE_CALENDAR_API_KEY="your_calendar_api_key_here"           # API key for Google Calendar
VITE_CALENDAR_ID="your_calendar_id_here"                     # Calendar ID to read events from

# Google Service Account configuration (server-side)
# Used for Calendar API and Gmail API (all email sending)
# Service account must have the following scopes enabled:
#   - https://www.googleapis.com/auth/calendar.readonly
#   - https://www.googleapis.com/auth/gmail.send
#
# By default, emails are sent directly from the service account.
# Set GMAIL_USE_DOMAIN_DELEGATION=true to use domain-wide delegation (optional).
# For domain-wide delegation (sending from specific users/groups):
#   In Google Workspace Admin Console, add service account with the scopes above
#   Then you can send from any user/group in your domain (e.g., contactus@lakeridepros.com)
GCAL_SA_EMAIL="your-service-account@your-project.iam.gserviceaccount.com"
GCAL_SA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
GMAIL_SENDER="contactus@lakeridepros.com"                    # Email address to send from (can be user or group email)
# GMAIL_USE_DOMAIN_DELEGATION="true"                        # Optional: Enable domain-wide delegation

# Firebase client configuration
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=
VITE_FIREBASE_VAPID_KEY="BA3D...your-key..."
VITE_ENABLE_ANALYTICS="true"
VITE_FIRESTORE_CACHE_MODE="persistent"

# OAuth client ID for Google sign-in
VITE_GOOGLE_CLIENT_ID="your_google_client_id_here"           # Google OAuth client ID

# Backend API configuration
VITE_API_BASE_URL="https://example.com/claim-proxy.php"      # Proxy endpoint for backend API
VITE_API_SECRET_KEY="your_api_secret_key_here"               # Shared secret for backend API

# CSV used for time log backup/import
VITE_TIME_LOG_CSV="https://example.com/time-log.csv"         # CSV export for time logs

# Cloud Function to move queued rides into live
VITE_DROP_DAILY_URL="https://us-central1-your_project_id.cloudfunctions.net/dropDailyRidesNow" # Cloud Function URL
# Optional if HTTPS function uses shared secret
VITE_LRP_ADMIN_TOKEN="changeme"                              # Admin token for secure function calls

# SMS configuration
VITE_SMS_MODE="prod"                                         # "test" or "prod"
VITE_SMS_DEFAULT_TO="+14173809953"                           # Fallback SMS recipient in prod
# VITE_SMS_TEST_TO="+18777804236"                            # Optional test mode recipient

# MUI X Pro license key
VITE_MUIX_LICENSE_KEY=

# Shuttle tickets email endpoint (optional)
# If not configured, the email button will download tickets as ZIP instead
# Example: https://us-central1-your-project-id.cloudfunctions.net/sendBulkTicketsEmail
VITE_TICKETS_EMAIL_ENDPOINT=

# Optional
VITE_ENABLE_FCM=
VITE_SHOW_DEBUG_PANELS=
VITE_SENTRY_DSN=
VITE_GAMES_ORIGIN=https://games.lakeridepros.xyz

# Note: Supabase credentials are configured in Cloud Functions (server-side only)
# No client-side env vars needed - Bolt uses secure Cloud Function proxy
# See functions/.env for backend configuration
